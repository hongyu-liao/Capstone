# Core dependencies
torch>=2.6.0
transformers>=4.45.0
accelerate>=0.33.0

# Docling for PDF processing with VLM support
docling>=2.40.0
docling-core>=2.40.0

# Web search (updated package name)
ddgs>=0.9.0

# Utilities
requests>=2.31.0
Pillow>=10.0.0
numpy>=1.24.0
pandas>=2.0.0

# Vision model support (fixed package names)
torchaudio>=2.6.0
torchvision>=0.19.0

# Model download and caching
huggingface-hub>=0.25.0

# Additional dependencies for SmolDocling
easyocr>=1.7.0
pypdf>=4.0.0

# Additional dependencies for better compatibility
safetensors>=0.4.0
tokenizers>=0.19.0

# MLX support for Apple Silicon (optional)
# mlx>=0.15.0